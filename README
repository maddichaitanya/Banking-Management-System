1. To run this application you need to install Apache NetBeans IDE and also install MYSQL for database creation and updates for user data.

2. Software need :-

   - java (JDK)
   - mysql (database)
   - Apache NetBeans (editor)

3. output:-
